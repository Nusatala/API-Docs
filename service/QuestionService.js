'use strict';


/**
 * Update question
 * This can only be done by the logged in admin.
 *
 * body Users Update an existent user in the Nusatala App (optional)
 * level String Quiz level for filltering the level of quizzes
 * id Integer id of the question
 * no response value expected for this operation
 **/
exports.updateQuestion = function(body,level,id) {
  return new Promise(function(resolve, reject) {
    resolve();
  });
}


/**
 * Update question
 * This can only be done by the logged in admin.
 *
 * body Users Update an existent user in the Nusatala App (optional)
 * level String Quiz level for filltering the level of quizzes
 * id Integer id of the question
 * no response value expected for this operation
 **/
exports.updateQuestion = function(body,level,id) {
  return new Promise(function(resolve, reject) {
    resolve();
  });
}

